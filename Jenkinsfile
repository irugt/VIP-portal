pipeline { 
    agent any  
    stages { 
        stage('Build') { 
            steps { 
               //mvn compile
               sh echo 'This is a minimal pipeline.'
            }
        }
    }
}
